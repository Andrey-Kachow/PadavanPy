// #include <SDL2/SDL.h>
#include <SDL.h>

SDL_Renderer *renderer;
SDL_Window *window;

int main(int argc, char *argv[])
{
	// memset(&app, 0, sizeof(App));

	// initSDL();

	// atexit(cleanup);

	while (1)
	{
		// prepareScene();

		// doInput();

		// presentScene();

		// SDL_Delay(16);
	}

	return 0;
}