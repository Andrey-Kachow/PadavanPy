0) Имена файлов и последовательности получать из аргументов командной строки.

1) Прочитать данные из файла. Запрещено считывать из файла каждый байт более одного раза.
   Чтение из файла осуществлять блоками размером 512 байт. Размер исходной строки не более 512 байт.
   В памяти нельзя хранить из файла данных одновременно больше, чем 1024 байт.

	- Функция открытия и чтения данных из файла. Проверка выводом.

2) Заменить вхождения одной последовательности байт на другую, результат записать в другой файл.

	- Алгоритм поиска и замены подстроки в строке (функция).
	- Запись строки в другой файл. Проверка.